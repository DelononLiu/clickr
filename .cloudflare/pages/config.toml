[build]
  command = "bun install && bun run build:web"
  publish = "public"

[build.environment]
  NODE_VERSION = "20"

# Force static site detection
[build.plugins]
  package = "@cloudflare/pages-plugin-static"
