# 环境变量配置

# OpenAI 配置（可选，不配置则需在界面中手动输入）
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-3.5-turbo

# 应用模式（可选，默认 auto）
# WEB: 强制 Web 模式
# ELECTRON: 强制 Electron 模式
# AUTO: 自动检测
CLICKR_MODE=auto
